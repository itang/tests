# example for iron + json restful

see https://github.com/brson/httptest

## Usage

```
$ cargo run --bin httptest
$ cargo run --bin client-get
$ cargo run --bin client-post
$ cargo run --bin client-get
```
